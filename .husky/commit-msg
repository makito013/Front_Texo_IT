#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx --no -- commitlint --edit ${1} ||
(
  echo '\n'
  echo '----------------------------------------------------------------------------------------------'
  echo '🤔🤔 OPA... mensagem do commit está fora do padrão... 🤔🤔'
  echo '----------------------------------------------------------------------------------------------'
  echo '\n'
  false;
)